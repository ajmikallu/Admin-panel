# Supabase Configuration
# These are safe to expose in frontend code (anon/publishable keys only)
# DO NOT include service keys or any secrets here!

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: Fallback to anon key if publishable key not available
# VITE_SUPABASE_ANON_KEY=
